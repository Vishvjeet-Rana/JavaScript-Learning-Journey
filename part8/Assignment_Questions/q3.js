// Create a new array whose elements are in uppercase of words present in the original array

let strArr = ["my", "country", "is", "best"];

const newStrArr = strArr.map((string) => string.toUpperCase());

console.log(newStrArr);
